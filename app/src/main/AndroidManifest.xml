<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_channel"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MerchTriB" >
        <activity
            android:name=".ui.activities.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.RegisterCompanyActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.AddTaskActivity"
            android:exported="false" />
        <activity android:name=".ui.activities.SendingActivity" />

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.ErrorActivity" />
        <activity android:name=".ui.activities.TaskActivity" />
        <activity android:name=".ui.activities.CheckGeoActivity" />
        <activity android:name=".ui.activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>